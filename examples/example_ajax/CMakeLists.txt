cmake_minimum_required(VERSION 3.18)
project(example_ajax)
add_executable(example_ajax main.cpp)
if(WIN32)
    target_link_libraries(example_ajax WebServer)
endif()
file(COPY dist DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/)
