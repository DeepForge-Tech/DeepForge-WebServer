cmake_minimum_required(VERSION 3.18)
project(example_dynamic)
add_executable(example_dynamic main.cpp)
if(WIN32)
    target_link_libraries(example_dynamic WebServer)
endif()
file(COPY dist DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/)
