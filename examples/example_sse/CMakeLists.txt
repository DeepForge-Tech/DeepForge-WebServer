cmake_minimum_required(VERSION 3.18)
project(example_sse)
add_executable(example_sse main.cpp)
if(WIN32)
    target_link_libraries(example_sse WebServer)
endif()
file(COPY dist DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/)
