cmake_minimum_required(VERSION 3.18)
project(example_static)
add_executable(example_static main.cpp)
if(WIN32)
    target_link_libraries(example_static WebServer)
endif()
file(COPY dist DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/)
